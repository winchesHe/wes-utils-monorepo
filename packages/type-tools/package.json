{
  "name": "@wes/type-tools",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsup --watch",
    "build": "tsc --noEmit && tsup --minify"
  },
  "devDependencies": {
  }
}